@model BDPFMA.Models.BdpfMc

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>BdpfMc</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="McId" />

            <div class="form-group">
                <label class="control-label">Cella</label>
                <select  asp-for="McCell" class="form-control" asp-items="ViewBag.McCell"></select>
            </div>

            <div class="form-group">
                <label class="control-label">Gép</label>
                <input asp-for="McName" class="form-control" />
                <span asp-validation-for="McName" class="text-danger"></span>
            </div>

            <div class="form-group">

                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
